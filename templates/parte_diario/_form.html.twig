<div class="form-container">
	<p class="title">Parte Diario</p>
{{ form_start(form) }}
<p class="title" style="text-align: start;">{{ empresa | title }}</p>
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Crear') }}</button>
{{ form_end(form) }}
    </div>
