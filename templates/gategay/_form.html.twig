<div class="form-container">
	<h1 class="title">OSDE</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Crear') }}</button>
{{ form_end(form) }}
</div>