<div class="form-container">
	<p class="title">Nuevo Archivo</p>
	{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
	    {{ form_widget(form) }}
	    <button class="btn">{{ button_label|default('Crear') }}</button>
	{{ form_end(form) }}
</div>

