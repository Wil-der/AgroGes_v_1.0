<div class="form-container">
	<p class="title">Especialidad</p>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Crear') }}</button>
{{ form_end(form) }}
</div>